services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: typing
      POSTGRES_PASSWORD: typing
      POSTGRES_DB: typing_game
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U typing -d typing_game"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
